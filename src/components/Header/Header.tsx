import React from 'react';
import {HeaderContainer, Title} from './Header.styles';

export const Header = () => {
    return (
        <HeaderContainer>
            <Title>Solid Investment Calculator!</Title>
        </HeaderContainer>
    );
};
